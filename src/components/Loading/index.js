// @import styles
import { LoadingContainer, LoadingContent, Label } from "./loadingstyled";

const Loading = () => {
  return (
    <LoadingContainer>
      <LoadingContent>
        Loading
        <Label />
      </LoadingContent>
    </LoadingContainer>
  );
};

export default Loading;
