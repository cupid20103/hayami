//@import styles
import { BorderContainer, BorderContent, BorderDraw } from "./border.styled";

const Border = () => {
  return (
    <BorderContainer>
      <BorderContent>
        <BorderDraw />
      </BorderContent>
    </BorderContainer>
  );
};

export default Border;
